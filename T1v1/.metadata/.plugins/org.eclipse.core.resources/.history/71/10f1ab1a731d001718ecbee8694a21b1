import java.util.*;

public class Pilha {

	  private List<Integer> objetos = new LinkedList<Integer>();

	  public void insere(Integer stack) {
	    this.stack.add(objeto);
	  }

	  public Object remove() {
	    return this.objetos.remove(this.objetos.size() - 1);
	  }

	  public boolean vazia() {
	    return this.objetos.size() == 0;
	  }
	}